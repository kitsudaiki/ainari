apiVersion: v1
kind: PersistentVolume
metadata:
  name: sakura-pv
spec:
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sakura-sc
  hostPath:
    path: /etc/ainari_data/
