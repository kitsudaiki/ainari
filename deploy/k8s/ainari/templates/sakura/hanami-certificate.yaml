apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sakura-selfsigned-cert
spec:
  secretName: sakura-selfsigned-secret
  issuerRef:
    name: selfsigned-issuer
  dnsNames:
    - {{ .Values.api.domain_sakura }}

