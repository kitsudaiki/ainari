apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: omamori-selfsigned-cert
spec:
  secretName: omamori-selfsigned-secret
  issuerRef:
    name: selfsigned-issuer
  dnsNames:
    - {{ .Values.api.domain_omamori }}

