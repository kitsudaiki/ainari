apiVersion: v1
kind: Service
metadata:
  name: miko-tls-service
  labels:
    run: miko
spec:
  selector:
    app: miko
  ports:
    - name: input
      protocol: TCP
      port: 8443
      targetPort: 8443
  type: ClusterIP
