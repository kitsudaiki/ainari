apiVersion: v1
kind: Service
metadata:
  name: torii-tls-service
  labels:
    run: torii
spec:
  selector:
    app: torii
  ports:
    - name: input
      protocol: TCP
      port: 8443
      targetPort: 8443
  type: ClusterIP
