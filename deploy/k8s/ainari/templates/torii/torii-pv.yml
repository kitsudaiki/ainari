apiVersion: v1
kind: PersistentVolume
metadata:
  name: torii-pv
spec:
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: torii-sc
  hostPath:
    path: /etc/ainari_data/
