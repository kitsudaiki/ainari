apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: torii-selfsigned-cert
spec:
  secretName: torii-selfsigned-secret
  issuerRef:
    name: selfsigned-issuer
  dnsNames:
    - {{ .Values.api.domain_torii }}

