apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: miko-selfsigned-cert
spec:
  secretName: miko-selfsigned-secret
  issuerRef:
    name: selfsigned-issuer
  dnsNames:
    - {{ .Values.api.domain_miko }}

