apiVersion: v1
kind: PersistentVolume
metadata:
  name: onsen-pv
spec:
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: onsen-sc
  hostPath:
    path: /etc/ainari_data/
