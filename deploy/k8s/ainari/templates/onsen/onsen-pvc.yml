apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: onsen-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storage_class }}
  resources:
    requests:
      storage: 100Mi
