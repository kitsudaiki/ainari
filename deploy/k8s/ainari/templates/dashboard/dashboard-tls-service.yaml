apiVersion: v1
kind: Service
metadata:
  name: dashboard-tls-service
  labels:
    run: dashboard
spec:
  selector:
    app: dashboard
  ports:
    - name: input
      protocol: TCP
      port: 8443
      targetPort: 8443
  type: ClusterIP
