apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard-selfsigned-cert
spec:
  secretName: dashboard-selfsigned-secret
  issuerRef:
    name: selfsigned-issuer
  dnsNames:
    - {{ .Values.api.domain_dashboard }}
