apiVersion: v1
kind: PersistentVolume
metadata:
  name: bento-pv
spec:
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: bento-sc
  hostPath:
    path: /etc/ainari_data/
