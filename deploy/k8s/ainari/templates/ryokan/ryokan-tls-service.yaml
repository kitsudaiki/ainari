apiVersion: v1
kind: Service
metadata:
  name: ryokan-tls-service
  labels:
    run: ryokan
spec:
  selector:
    app: ryokan
  ports:
    - name: input
      protocol: TCP
      port: 8443
      targetPort: 8443
  type: ClusterIP
