apiVersion: v1
metadata:
  name: ryokan-config
data:
  ryokan.toml: |+
    debug = true
    log_path = ""
    insecure_clients = true

    [storage]
    dataset_location = "/etc/ainari/ryokan_data/datasets"
    checkpoint_location = "/etc/ainari/ryokan_data/checkpoints"
    tempfile_location = "/etc/ainari/ryokan_data/tempfiles"

    [api]
    public_ip = "127.0.0.1"
    public_port = 11416
    internal_ip = "127.0.0.1"
    internal_port = 10416
    internal_api_key = "{{ .Values.api.internal_api_key }}"

    [database]
    file_path = "/etc/ainari/ryokan/ryokan_db"
    
    [miko]
    address = "https://miko-tls-service.default.svc.cluster.local"
    port = 8443

kind: ConfigMap
