[package]
name = "ainari_hardware"
version = "0.1.0"
edition = "2024"

[dependencies]
# read some hardware-information
sysinfo = "0.37.0"
# json
serde = { version = "1", features = ["derive"] }
serde_json = "1"
