[package]
name = "hanami_cluster_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
# parser-generator
pest = "2"
pest_derive = "2"

# json
serde = { version = "1", features = ["derive"] }
serde_json = "1"
