[package]
name = "hanami_hardware"
version = "0.1.0"
edition = "2024"

[dependencies]
# read some hardware-information
sysinfo = "0.36.1"
# json
serde = { version = "1", features = ["derive"] }
serde_json = "1"
