version: "3.9"
services:
  dashboard:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./app:/app
      - /app/node_modules
      - /etc/ainari/dashboard_config.json:/app/public/config.json
# docker compose up --build
